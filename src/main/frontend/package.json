{
  "name": "frontend",
  "version": "1.0.0",
  "scripts": {
    "build": "tailwindcss -i ./app.css -o ../resources/static/app.css",
    "watch": "tailwindcss -i ./app.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.2.4"
  }
}
